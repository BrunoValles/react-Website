import React from 'react';
import {SidebarContainer, Icon, ColseIcon} from './SidebarElements';

const Sidebar = ( ) => {
    return(
      <SidebarContainer>
	<Icon>
	  <CloseIcon />
	</Icon>
      </SidebarContainer>
    );
}

export default Sidebar;
